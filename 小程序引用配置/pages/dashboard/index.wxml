<view class="logo">
    <text class="dashboard-title">3333</text>
</view>
<view class="lists">
	<div>sdfsdfsdfsdfsdf</div>
    <block wx:for="{{lists}}" wx:key="" wx:item="item">
        <navigator class="list-item" url="{{item.url}}" hover-class="navigator-hover">{{item.name}}</navigator>
    </block>
</view>
